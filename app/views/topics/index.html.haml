%h1 トピック一覧
%ul
  - @topics.each do |topic|
    %li= link_to topic.title, topics_show_path(topic.id)
    = link_to '[削除]', topic_delete_path(topic.id), method: :delete, data: {confirm: "削除してもよろしいですか？"}
= link_to "新規", topics_new_path